{
  "name": "one-month-one-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate:og": "tsx scripts/generate-og-cards.ts",
    "generate:myindie": "tsx scripts/generate-myindie-banners.ts",
    "generate:artwork": "npm run generate:og && npm run generate:myindie",
    "sync": "tsx scripts/sync-games.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@tailwindcss/vite": "^4.1.0",
    "@types/node": "^22.0.0",
    "daisyui": "^5.0.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.1.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
